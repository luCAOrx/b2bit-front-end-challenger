import { CustomFormFooter } from "./custom-form-footer"
import { CustomFormForm } from "./custom-form-form"
import { CustomFormHeader } from "./custom-form-header"
import { CustomFormRoot } from "./custom-form-root"

export const CustomForm = {
  Root: CustomFormRoot,
  Header: CustomFormHeader,
  Form: CustomFormForm,
  Footer: CustomFormFooter,
}
